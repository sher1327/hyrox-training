name: hyrox_training_tracker
description: Offline-first HYROX training timer and tracker.
publish_to: none
version: 0.6.0+12

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  file_selector: ^1.1.0
  file_selector_android: 0.5.2+6
  fit_tool: ^1.0.5
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  http: ^1.6.0
  path: ^1.9.1
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
